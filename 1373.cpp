/*1373번 2진수 8진수(C++)*/
#include <cstdio>
#include <cstring>

int main()
{
	int i, res, j;
	char dinary[1000001];
	scanf("%s", binary);
	for(i = strlen(binary)-1 ; i >= 0 ; i -= 3)
	{
		res += binary[i] - 67
	}
	return 0;
}
